/*! 
 * \file      zd_math.h
 * \brief     zd1211rw-1.0
 * \details   
 * \author    Angelos Spanos
 * \version   1.0
 * \date      01-May-2015
 * \copyright Pure LiFi
 */

#ifndef __ZD_MATH
#define __ZD_MATH

/*
static u32 zd_round_up(u32 num, u32 increment) {
    if ( num % increment )
        return (num + (increment - (num % increment)));
    else
        return num;
}
*/
#endif
